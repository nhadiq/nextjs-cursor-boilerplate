---
description: ESLint — fix violations in source; never disable rules or edit config to bypass
globs: **/*.ts, **/*.tsx, **/*.js, **/*.jsx, eslint.config.*
alwaysApply: false
---
Always fix lint by correcting on code, #Never use scripts or alternates and edit the lint code or the builds to work around the error